# GitHub Profile Telemetry v10

This repository powers the live developer telemetry card shown on the `iMoein` GitHub profile.

The dashboard combines two data sources:

1. GitHub-side metrics generated by `scripts/generate_profile.py`
2. A privacy-safe daily snapshot produced by a local macOS LaunchAgent

The public README intentionally stays minimal and renders only the generated SVG.

## Dashboard sections

- `runtime // identity` — profile identity, platforms, kernel, uptime, Unix-style age counter
- `github // activity` — account age, active days, first/last activity, contributions, visible repositories
- `toolchain // stack` — editor, languages, tools, technical focus
- `codebase // inventory` — aggregate code lines, tracked code files, repositories scanned, top languages
- `yesterday // development activity` — commits, added/deleted lines, net change, active repositories

## Daily activity privacy model

Only aggregate numbers are published in `stats/yesterday.json`.

The snapshot does **not** publish repository names, private repository metadata, local file paths, filenames, commit messages, credentials, tokens, IP addresses, hostnames, Wi-Fi information, or process data.
## Automated flow

```text
macOS LaunchAgent
      |
      +--> read installed VS Code version
      |
      +--> query owned GitHub repositories
      |
      +--> aggregate previous-day Git additions/deletions
      |
      +--> update profile.json + stats/yesterday.json
      |
      +--> commit and push
                |
                v
        GitHub Actions
                |
                +--> collect GitHub profile/codebase metrics
                +--> regenerate assets/terminal.svg
                +--> refresh README cache-buster
```

GitHub Actions is event-driven for telemetry/config changes and also runs once per day as a safety refresh. Generated SVG/README commits do not recursively trigger the workflow.

## GitHub token

Public repository metrics work with `GITHUB_TOKEN`.

For private repository coverage, configure the repository secret:

```text
PROFILE_STATS_TOKEN
```

The token should have read access only to repositories that should contribute to aggregate statistics.
## Local regeneration

Install dependencies:

```bash
python3 -m pip install -r requirements.txt
```

Generate the dashboard:

```bash
python3 scripts/generate_profile.py
```

The generator reads `profile.json` and, when present, `stats/yesterday.json`. Repository line scanning uses a bounded parallel clone pool (`max_parallel_clones`, default `4`) to reduce refresh time without creating excessive concurrent Git traffic.

## Local macOS automation

The local automation uses:

```text
$HOME/Scripts/publish-github-profile-telemetry.sh
$HOME/Scripts/collect-github-yesterday.py
$HOME/Library/LaunchAgents/com.imoein.github-profile-telemetry.plist
```

Install or refresh the macOS agent from the repository:

```bash
bash scripts/local/install_launchagent.sh
```

The LaunchAgent checks hourly, but a successful telemetry publish is limited to once per local calendar day. If GitHub is unreachable, the run is skipped and retried later rather than marking the day complete.

The versioned agent source lives under `scripts/local/`. The installer deploys runtime copies into `$HOME/Scripts`, migrates the legacy `editor-sync` LaunchAgent name, validates the generated plist, and reloads the job.

GitHub credentials are read through the existing macOS Git credential helper. No token is stored in this repository.
## Daily snapshot semantics

The previous local calendar day is converted to UTC boundaries for GitHub API queries.

Only commits attributed by GitHub to the profile owner are counted. Merge commits are excluded to avoid double-counting merged history.

The profile repository itself is excluded from daily development totals so that telemetry automation does not inflate its own activity.

Line counts are Git diff statistics, not a productivity score. Generated files, dependency lockfiles, formatting changes, and large refactors can legitimately produce large values.

## Repository hygiene

Generated/public artifacts:

- `assets/terminal.svg`
- `README.md`
- `stats/yesterday.json`

Local-only files such as `.DS_Store`, Python caches, virtual environments, and environment files are ignored by `.gitignore`.
